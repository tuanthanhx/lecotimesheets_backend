{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "php artisan serve --host 192.168.1.15 --port=3000",
        "cache": "php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan view:clear",
        "migrate": "php artisan migrate",
        "seed": "php artisan db:seed"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
