{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "php artisan serve --host localhost --port=3000"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
